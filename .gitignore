# nvim
**/nvim-pack-lock.json
**/lazy-lock.json

# spicetify
**/spicetify/config-xpui.ini
**/spicetify/user.css

# herdr runtime state (only config.toml is tracked)
herdr/.config/herdr/*
!herdr/.config/herdr/config.toml

# hyprpaper writes its config into the stowed (folded) hypr dir
hypr/.config/hypr/hyprpaper.conf

# macOS
**/.DS_Store

# secrets & local overrides
.env
#*.local
